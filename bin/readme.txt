When in toplevel is done :

    mkdir build
    cd build
    cmake ..
    make

Then the files :

    halcmd
    module_helper

Need root privileges. Todo so :

    ./setroot
    
To add the ~/lib dir to our system search path :

	~halcore/lib/$ sudo ldconfig 




